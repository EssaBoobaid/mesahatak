<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@fortawesome/fontawesome-free@6.5.1/css/all.min.css">
  <link rel="stylesheet" href="/css/review.css">
</head>
<body class="review-page">
  <%- include('../partials/navbar', { lang: 'ar', active: 'reviews', user: typeof user !== 'undefined' ? user : null }) %>
  <main class="reviews-shell">
    <section class="card">
      <div class="card-header">
        <div>
          <p class="eyebrow">تقييماتي</p>
          <h1>تعليقاتي على المساحات</h1>
          <p class="muted">عرض وإدارة تقييماتك السابقة.</p>
        </div>
        <a class="btn primary" href="/dashboard/my-booking"><i class="fa-solid fa-arrow-right-from-bracket"></i><span>عودة إلى حجوزاتي</span></a>
      </div>

      <% if (typeof errorMessage !== 'undefined' && errorMessage) { %>
        <div class="alert"><i class="fa-solid fa-triangle-exclamation"></i><%= errorMessage %></div>
      <% } %>

      <div class="review-list">
        <% if (reviews && reviews.length) { %>
          <% reviews.forEach(r => { %>
            <article class="review-item">
              <div class="review-head">
                <strong><%= r.Space ? r.Space.name : 'مساحة' %></strong>
                <div class="stars">
                  <% for (let i = 0; i < (r.rating || 0); i++) { %><i class="fa-solid fa-star"></i><% } %>
                </div>
              </div>
              <p class="review-body"><%= r.comment || '' %></p>
            </article>
          <% }) %>
        <% } else { %>
          <p class="muted">لا توجد تقييمات بعد.</p>
        <% } %>
      </div>
    </section>
  </main>
  <%- include('../partials/footer') %>
</body>
</html>
